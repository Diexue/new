export default{
  items:state=>state.items,
  // selectItems:state=>state.selectItems,
  hasLogin:state=>state.hasLogin,
  count:state=>state.count
  // count:state=>{console.log('s->,',state.count);return state.count+=1}
}
